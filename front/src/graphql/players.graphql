query Players($take: Int, $skip: Int) {
    players(take: $take, skip: $skip) {
        id
        lastName
        name
        playerEmail
    }
}

query PlayersCount {
  aggregatePlayer {
    _count {
      id
    }
  }
}
