query Players($take: Int, $skip: Int) {
    players(take: $take, skip: $skip) {
        id
        lastName
        name
    }
}

query PlayersCount {
  aggregatePlayer {
    _count {
      id
    }
  }
}
