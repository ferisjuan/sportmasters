declare namespace notification {
    type Type ={
        id: string
        title: string
        color: string
    }

    enum ENUM {
        SUCCESS = 'SUCCESS',
        ERROR = 'ERROR',
        WARNING = 'WARNING',
        INFO = 'INFO',
    }
}
